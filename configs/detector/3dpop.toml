# 3dpop: ONE val session, 16 labelled frames. Shipped `val_frames_per_group = 8` samples 50% of
# it -- cheap to remove entirely. Raised to the group's own labelled length
# (dev/plans/detector_accuracy.md T0.3).
extends = "../detector.toml"

[data]
path = "/groups/karashchuk/karashchuklab/animal-datasets-processed/tailcycle-datasets/3dpop"
val_frames_per_group = 16

[training]
out = ""
