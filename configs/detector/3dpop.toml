# 3dpop: ONE val session, 16 labelled frames. Raised to the group's own labelled length (the
# shipped 8 sampled only half of it).

[data]
path = "/groups/karashchuk/karashchuklab/animal-datasets-processed/tailcycle-datasets/3dpop"
val_frames_per_group = 16

[training]
out = ""
