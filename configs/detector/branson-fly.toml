# branson-fly: ONE val group, 32 labelled frames; raised to the group's own labelled length (the
# shipped 8 sampled only a quarter -- cheap here).

[data]
path = "/groups/karashchuk/karashchuklab/animal-datasets-processed/tailcycle-datasets/branson-fly"
val_frames_per_group = 32

[training]
out = ""
